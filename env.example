# Google Places API Key (server-side only - for build-time data fetching)
# ⚠️  CRITICAL: NEVER add PUBLIC_ prefix to this key (security risk)
PLACES_API_KEY=your_places_api_key_here

# Google Maps API Key (client-side - for interactive map display)
# This can be seen by users but should be restricted to your domain
PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here 